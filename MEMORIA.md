# MEMORIA.md — TopoField, estado de decisiones (rework)

**Última actualización:** 31 de julio de 2026
**Rol de este archivo:** punto de entrada único para saber qué se decidió, qué se verificó y qué sigue pendiente sobre el rework de TopoField. Si hay contradicción entre este archivo y un resumen anterior (incluida cualquier auditoría externa), **manda lo verificado aquí**, con fecha de verificación.

Documentos relacionados:
- `TOPOFIELD_PLAN_MAESTRO_REWORK_2026-07-26.md` (fuera del repo, en la carpeta compartida) — plan de rework auditado con Codex/ChatGPT/Claude.
- `TOPOFIELD_ADDENDA_APROBACION_2026-07-26.md` (fuera del repo) — enmiendas 1–5 al plan anterior.
- `PLAN.md` (este repo) — plan de producto/UX anterior, centrado en piloto y validación de usabilidad. **No sustituido**, pero desactualizado respecto al estado real de Supabase (ver §3).
- `PRODUCT_STRATEGY.md`, `UX_RESEARCH_PLAN.md`, `LAUNCH_PLAN.md` — siguen vigentes como referencia de producto/UX, no verificados en esta sesión.

---

## 1. Decisión tomada

**El plan maestro de rework está aprobado, con 5 enmiendas.** No se reescribe la aplicación. Se hace un rework por capas empezando por reconciliar el estado real (§3 de este archivo, ampliado hoy) antes de tocar nada.

Resumen de las enmiendas aprobadas:

1. **Validación de usuario:** el autor es el usuario piloto desde la semana 1; el piloto de varios trabajadores se pospone, no se cancela.
2. **Modelo de faenas reducido:** 5 tablas en vez de 10 (`work_items`, `project_work_items`, `work_events` append-only, `work_evidence`, `import_batches`). Se pospone `work_plans`/`work_occurrences`/`work_reviews` hasta que alguien pida reasignación o aprobación.
3. **Presupuesto de tiempo:** 5–10 h/semana declaradas. Fases 0→3 en un trimestre. El aislamiento multi-tenant (RLS) sustituye al planificador como prioridad, porque hay intención de venta a terceros.
4. **Regla de agentes:** un solo agente escribiendo en una rama a la vez; se descarta la jerarquía "Codex ejecutor / Claude revisor" (era circunstancial, no una capacidad real).
5. **Riesgo de titularidad:** el catálogo de faenas y los nombres de obra son activos operativos del empleador; los datos medidos pertenecen a terceros (clientes de auscultación). Ver §5.

Evaluación honesta de la app antes de esta expansión, con instrumentos aún sin terminar (solo estaciones/prismas/incidencias/guía/fotos, desplegado en producción): **6.5/10 como primera app**. Bien: backend en capas, migraciones incrementales, tests de backend, `expo-secure-store`, RLS realmente activo en producción (ver corrección en §4). Mal: sin cola offline pese a ser la promesa central, cero tests móviles, `Station` mezcla conceptos, datos reales de obra commiteados en el historial de Git.

---

## 2. Corrección — TopoTask no es contaminación

Una hipótesis previa (mía, basada en una auditoría externa sin acceso al repo) decía que TopoField y TopoTask compartían base de datos por error. **Verificado hoy contra Supabase real: es falso.**

- `topofield` (id `tmlexrsnxpmykbpeebri`, creado 2026-05-29, eu-north-1) y `topotask-backend` (id `jwckuoiossieiyankkvh`, creado 2026-06-13, eu-west-1) son **dos proyectos Supabase separados**, sin base compartida.
- El único vínculo real es una importación deliberada y cuidadosa (mayo 2026, documentada en `C:\Users\guill\Documents\Aplicacion_Movil\MEMORY.md` líneas ~452–514): `apps/backend/src/scripts/import-topotask-prisms.ts`, dry-run por defecto, deduplicación por hash, 171 prismas candidatos, 1889 lecturas históricas importadas a `prisms`/`prism_observations`. Es una migración de datos legítima, no un descuido.

No excluir estos datos de futuros análisis. Sí mantener la regla general de parar y preguntar cuando aparezcan datos de un proyecto distinto sin anunciar — esta vez la duda estaba justificada, la conclusión inicial no.

---

## 3. Hallazgo crítico — deriva de esquema no versionada (verificado 2026-07-26)

Esto es más grave que "6 commits locales por delante de `origin/main`" (que sigue siendo cierto y sigue pendiente de resolver). Verificado con las herramientas de Supabase, de solo lectura:

**Corrección importante (26-07-2026, tras el informe de Fase 0 de Claude Code):** dije antes "0 filas en todas, sin riesgo de datos". Es falso — verificado con `COUNT(*)` real, no con el campo `rows` de `list_tables` (que es una estimación estadística de Postgres, `pg_class.reltuples`, y puede mostrar 0 aunque haya datos reales sin actualizar). Conteos reales:

| Tabla | Filas reales |
|---|---:|
| `sensores` | 72 |
| `mediciones` | 145 |
| `estacionamientos` | 22 |
| `profiles` | 11 |
| `campanas` | 7 |
| `obras` | 6 |
| `total_station_imports` / `total_station_observations` | 2 (datos de prueba) |
| `jornadas`, `leveling_imports` | 0 |

**Consecuencia:** la familia "obras" (`obras`/`campanas`/`jornadas`/`sensores`/`mediciones`/`estacionamientos`) no es un experimento vacío — son 76 semanas-obra de datos operativos reales. Cualquier decisión de adaptar, migrar o descartar esta familia exige la misma disciplina de no-sobrescritura-silenciosa que el resto del proyecto (ver regla de oro metrológica del plan maestro). Ya no es una decisión de "cero riesgo".

**Lección de método:** para verificar filas reales en Supabase, usar `execute_sql` con `COUNT(*)`, nunca el campo `rows` de `list_tables`.

**8 migraciones aplicadas directamente en Supabase, sin archivo correspondiente en `apps/backend/migrations/`:**

| Versión | Nombre |
|---|---|
| 20260621124104 | total_station_staging |
| 20260621130517 | total_station_commit_traceability |
| 20260621131650 | commit_total_station_import_rpc |
| 20260621131925 | harden_total_station_commit_rpc_sequences |
| 20260621184721 | rollback_total_station_commit_batch_rpc |
| 20260622050516 | grant_total_station_commit_sequence_update |
| 20260627061243 | allow_campaign_delete_for_total_station_commit_history |
| 20260627061836 | allow_import_cleanup_for_total_station_commit_history |

**14 tablas en producción sin correspondencia en `shared/types.ts` local ni en las migraciones 001–014 versionadas:**

`obras`, `sensores`, `jornadas`, `campanas`, `estacionamientos`, `mediciones`, `incidencias`, `incidencia_fotos`, `obra_destinatarios`, `envios_correo`, `profiles`, `station_readings`, `work_sessions`, `capture_logs` — más toda la familia `total_station_*` (`total_station_imports`, `total_station_setups`, `total_station_observations`, `total_station_reduced_points`, `total_station_reduced_point_observations`, `total_station_commit_batches`, `total_station_commit_links`) y `leveling_*` (`leveling_imports`, `leveling_import_rows`, `leveling_import_issues`, `leveling_import_commits`).

**Lectura de esto:** ya existe en producción, construido por otra vía (probablemente Codex trabajando directo contra Supabase en otra sesión, sin dejar commit en git), buena parte de lo que el plan maestro propone construir — nivelación, estación total, e incluso una versión en español de "obras/jornadas/mediciones" que se parece al patrón que describiste del Excel (obra → faena por instrumento → cierre). **No se puede escribir un plan de "qué construir" sin inventariar primero qué de esto ya existe, qué sirve, y qué se descarta.**

**Esto se convierte en la ampliación obligatoria del encargo de Fase 0** (ver `TOPOFIELD_ADDENDA_APROBACION_2026-07-26.md` §7): antes de diseñar cualquier tabla nueva de faenas, inventariar estas 8 migraciones y 14+ tablas con detalle de columnas, políticas RLS reales y relaciones — sin aplicar nada, sin modificar nada.

---

## 4. Corrección — RLS sí está activo

Dije antes que las migraciones no tenían RLS y por tanto "no había RLS verificado". Eso era cierto solo de los **archivos locales** (001–014, cero sentencias `ENABLE ROW LEVEL SECURITY`). Verificado hoy contra la base real: **RLS está activo en las 37 tablas del proyecto `topofield`** (`rls_enabled: true` en todas, incluidas las 14 tablas no versionadas de §3). El advisor de seguridad de Supabase solo señala un aviso menor: protección de contraseñas filtradas desactivada (`auth_leaked_password_protection`, nivel WARN).

**Lo que sigue siendo cierto y sigue siendo un problema:** esas políticas RLS no están en ningún archivo versionado — existen solo en la base. Si se restaura el proyecto desde los archivos del repo, esas políticas no se recrean. Sigue pendiente volcarlas a un `NNN_rls_baseline.sql` de solo lectura (sin aplicar), tal como pedía la enmienda del encargo de Fase 0.

---

## 5. Riesgo de titularidad y exposición — estado

- Repositorio **privado** (confirmado por Erick, 26-07-2026) — baja la urgencia de exposición pública inmediata.
- Nombres reales de obra (***REMOVED***, ***REMOVED***, ***REMOVED***, etc.) están commiteados en `origin/main`, no solo en local (`apps/backend/migrations/002_projects_and_station_project_fk.sql`, `data/mapEst/stations.json`, `data/project-memberships.json`, varios `.md`).
- **"Purgar el historial de Git" en simple:** Git guarda cada versión de cada commit para siempre; borrar el archivo hoy no borra las versiones antiguas donde ya existía. Purgar significa reescribir el historial completo (herramienta: `git filter-repo`) para que esos datos nunca hayan estado ahí — cambia todos los hashes de commit y exige forzar esa versión sobre GitHub. Es una operación destructiva y no se ejecuta sin autorización explícita en el momento.
- **Disparador de esa purga:** antes de la primera demo a un cliente potencial, antes de dar acceso a un colaborador externo, o antes de plantear abrir el repositorio. No es urgente hoy porque el repo es privado.
- **Corrección (01-08-2026):** Erick aclaró explícitamente que no existe relación de empleador — el proceso de las 48 faenas es propio, de su práctica profesional/negocio, no de un tercero empleador. Esto revierte la nota anterior (que databa de una sesión previa, y que el propio Erick había confirmado hace unos mensajes en esta misma conversación al elegir "las 48 faenas reales del empleador" como respuesta). Con esta corrección, la capa (2) de abajo ya no requiere permiso de empleador.
- Tres capas de datos a distinguir (no dos): (1) patrón funcional genérico obra→faena→check — libre de usar; (2) proceso propio de Erick (48 faenas, frecuencias) — libre de usar según su aclaración del 01-08-2026, sin relación de empleador; (3) datos de terceros (clientes reales de auscultación cuyos activos se miden — ej. operador de infraestructura si `Línea 8`/estaciones reales pertenecen a un tercero) — esta capa sigue sin resolverse explícitamente y es distinta de la (2): no depende de si hay empleador, depende de quién es dueño del activo real que se monitorea.
- Ya no pendiente: permiso de empleador (no aplica, no existe esa relación según aclaración de Erick).
- Sigue abierto, sin urgencia mientras el catálogo semilla del producto siga siendo genérico: si en algún momento se usan los datos reales de las 48 faenas (nombres de activos reales) en algo que vea un tercero (cliente, colaborador, comprador del producto), confirmar si esos activos pertenecen a un cliente de auscultación que también necesitaría dar su visto bueno — pregunta distinta a la del empleador, no resuelta todavía.

---

## 6. Siguiente paso único (revisado)

El encargo de investigación de solo lectura de la Fase 0 (ver adenda §7) se amplía con un punto nuevo, y pasa a ser el más importante de la lista:

> Inventariar las 8 migraciones no versionadas (`total_station_*`) y las 14+ tablas sin correspondencia local (`obras`, `jornadas`, `mediciones`, `campanas`, etc.): qué columnas tienen, qué políticas RLS reales aplican, si tienen datos (hoy: 0 filas en todas, según `list_tables`), y si conviene versionarlas retroactivamente como migraciones locales o descartarlas.

Nada se aplica, nada se modifica, hasta cerrar ese inventario.

**Evidencia de que este archivo está al día:** cada sección lleva su propia verificación (`list_projects`, `list_tables`, `list_migrations`, `get_advisors`, `git log`) ejecutada el 26-07-2026. Si en una sesión futura estos hechos no coinciden con lo que se observa, gana lo observado, y este archivo se corrige de nuevo con la fecha nueva — igual que se corrigió hoy la hipótesis de TopoTask.

---

## 7. Decisión pendiente — fisurómetro, extensómetro, clinómetro

Erick preguntó (26-07-2026) si estos tres instrumentos estaban contemplados. Verificado:

- **No están diseñados** en el plan maestro ni en la adenda. Solo 4 disciplinas tienen estructura propia: nivelación, estación total, inclinometría, cuerda vibrante/piezómetro. Extensómetro aparece una vez, como referencia a un producto SISGEO, no como disciplina.
- **Sí existe una etiqueta genérica en el código ya desplegado** (`shared/types.ts`, `InstrumentType`, y `apps/backend/src/utils/monitoring-validation.ts`): 7 valores (`total_station`, `digital_level`, `piezometer`, `distometer`, `linometer`, `inclinometer`, `cant_rule`). `distometer`/`linometer` no son nombres estándar del sector — probablemente un intento rápido de traducir fisurómetro/clinómetro sin asentar vocabulario.
- **Hallazgo más importante:** detrás de los 7 tipos hay una sola tabla de lectura genérica (`createInstrumentReadingSchema`: `valueNumeric | valueText` + `unit` texto libre + `rawPayload` JSON). Es exactamente el antipatrón que el propio plan prohíbe en §8 ("un único InstrumentReading(value, unit) no es suficiente") — y ya está construido en migración 014 (monitoring rounds MVP, aún no publicada en `origin/main`), no solo propuesto. Ni inclinómetro ni piezómetro usan hoy una estructura propia real pese a estar "diseñados" en el papel.

**Familia estructural (para cuando llegue el momento, no ahora):** fisurómetro, extensómetro y clinómetro miden desplazamiento o inclinación relativa entre puntos fijos de referencia (mm o grados) — familia distinta de inclinómetro (perfil de profundidad en sondeo) y piezómetro (frecuencia/presión). Probablemente comparten una estructura de observación con validación propia por tipo, no una tabla por instrumento ni el blob genérico actual.

**Nivelación digital — equipos concretos (recordatorio de Erick, 29-07-2026):** integrar nivelación digital con los niveles reales que usa: **Leica LS10, LS15 y DNA03**. A diferencia de fisurómetro/extensómetro/clinómetro, "nivelación" ya es una de las 4 disciplinas con estructura propia contemplada en el plan maestro (§7 arriba) y tiene su propio valor en `InstrumentType` (`digital_level`) — pero eso es solo la etiqueta; no hay todavía diseño de cómo entra el dato específico de estos tres equipos (formato de exportación de cada uno, si se importa por archivo o se teclea a mano, columnas reales de una nivelada). **No decidido todavía:** el orden exacto dentro de Fase 6 frente a piezómetro/inclinómetro — cuando llegue el momento de abrir Fase 6, hay que decidir si nivelación digital va primero (ya tiene estructura contemplada) o se mantiene el orden ya escrito en §8 (piezómetro/inclinómetro primero). Sigue sin ser ahora: Fase 3 (faenas) va antes que Fase 6.

**Decisión:** no diseñar ni construir tablas para estos tres ahora. El plan exige que una disciplina complete una campaña real antes de abrir la siguiente (Fase 6), y ni piezómetro ni inclinómetro lo han hecho todavía. Construir esto ahora sería la sobreingeniería que el plan advierte evitar.

**Contexto real de formato, extraído de Argos (29-07-2026):** Erick tiene un proyecto aparte, Argos (`C:\Users\guill\.argos`), que ya valida archivos reales de nivelación de estos mismos equipos con datos de campo reales (proyecto "***REMOVED***"). Esto NO se mezcla con TopoField ni se importa código — es solo contexto verificado para cuando llegue el diseño de Fase 6:

- **DNA03 exporta `.CST`** (formato "banco" clásico Leica). Cabecera: `I  DNA03  <número de serie>` (ej. `338035`, `339049` — dos equipos DNA03 distintos ya usados en la misma obra). Bloques de sesión `L EF LINE#####` con filas de medida `N` (columnas: tipo de medida, punto, hora, mira/staff, distancia, temperatura, modo, nº repeticiones, desviaciones sdObs/sdMean). Opcionalmente aparece un bloque de calibración `L A x x B Chek&Adj` con secuencia de puntos A1→B1→B2→A2. El error de colimación viene embebido como línea `* ColimError EarthCurvCorr` seguida de un valor numérico y Sí/No (si se aplicó corrección de curvatura terrestre).
- **LS10 y LS15 exportan `.ASC.txt`** — formato distinto al `.CST` de DNA03, aunque comparten la misma cabecera `I <modelo> <número de serie>` y la misma línea de `ColimError`/`EarthCurvCorr`. Confirmado con archivos reales: `M_260708ZIT.ASC.txt` (`LS10`, serie `708106`) y `M_260705P.ASC.txt` (`LS15`, serie `711098`).
- **Implicación directa para el diseño futuro:** hacen falta al menos **dos parsers distintos** (CST para DNA03, ASC para LS10/LS15), no uno universal. Argos, según su propio informe de auditoría interna (Fase 4.0), encontró que ni siquiera dentro del mismo modelo (DNA03) el formato es 100% uniforme: algunos archivos traen el bloque `Chek&Adj` completo y otros solo el valor `ColimError` suelto — la recomendación de Argos fue no asumir nunca que ese bloque existe, y distinguir "valor de colimación disponible" de "ensayo de colimación estructurado verificable". Aplica igual si TopoField valida colimación en el futuro.
- **Colimación es específica del equipo físico, no solo del modelo:** Argos ya modela referencias de colimación por (línea, zona, instrumento) y detecta explícitamente si se usa una referencia de un instrumento incompatible (ej. una referencia de LS15 contra un archivo DNA03). Señal a tener en cuenta si TopoField llega a comparar colimaciones entre campañas.

**Se añade al inventario de §6:** revisar si el blob genérico de `createInstrumentReadingSchema` debe entrar en el mismo inventario de deriva de esquema, ya que es el mismo problema de fondo (estructura genérica donde el dominio exige estructura propia).

---

## 8. Plan de fases consolidado (2026-07-26)

Este plan **sustituye** la lista de fases de `TOPOFIELD_PLAN_MAESTRO_REWORK_2026-07-26.md` para efectos de ejecución (las enmiendas de la adenda ya están incorporadas). `PLAN.md` sigue vigente como referencia de producto/UX donde no contradiga esto.

| Fase | Objetivo | Cambio respecto al plan original |
|---|---|---|
| **0 — Reconciliación total** | Una sola verdad antes de programar | **Ampliada.** Ya no es solo git vs `origin/main`. Incluye inventariar las 8 migraciones y 22+ tablas de §3 (0 filas cada una, sin riesgo de datos) y decidir por tabla: aprovechar/adaptar/descartar. Salida añadida: `015_rls_baseline.sql` con las políticas reales. |
| **1 — Contrato de producto y dominio** | Diseñar antes de crear tablas | ⚠️ **ADR 001 SUPERSEDIDO (29-07-2026) — la decisión de adoptar `obras/campanas/jornadas/sensores/mediciones/estacionamientos` se basó en un error: esas tablas están en el proyecto Supabase `topotask-backend` (`jwckuoiossieiyankkvh`), NO en `topofield` (`tmlexrsnxpmykbpeebri`, el que usa `apps/backend/.env`). El backend real nunca pudo leerlas. Decisión de Erick: diseñar el modelo de faenas desde cero dentro de `topofield`, sin migrar `obras`.** Ver ADR 001 (nota de corrección al inicio) y §9/§12 de hoy. Limpieza de `profiles`→`users` y 4 tablas TopoTask sí fue real y correcta (esas sí estaban en `topofield`). Catálogo semilla genérico/real (enmienda 5) sigue pendiente. |
| **2 — Base fiable y offline** | Motor que usan todas las operaciones | **COMPLETADA Y VALIDADA EN DISPOSITIVO REAL (29-07-2026).** SQLite versionado, outbox persistente, sync engine global (arranca con la app, no al entrar en una estación), retry/backoff/conectividad y estados pending/syncing/synced/error/conflict. Hermes usa UUID nativo mediante `expo-crypto`. Resultado final: 28/28 tests móviles, 23/23 backend, TypeScript limpio, dos mensajes E2E y una prueba endurecida de reinicio sin túnel backend; los tres quedaron `synced` en SQLite y una sola vez en Supabase con `client_request_id`. La repetición controlada del segundo UUID devolvió el mismo registro sin duplicarlo. Informe: `PHASE_2_DEVICE_E2E_REPORT_2026-07-29.md`. |
| **3 — MVP de faenas semanales** | Sustituir el Excel | **Implementada y validada técnicamente en Galaxy (31-07-2026).** `monitoring_rounds` y RLS (`019`/`020`) están aplicadas; Render publica `e7e2902`; móvil cubre rondas, puntos, lecturas, histórico y foto offline-first. El E2E final creó una lectura y foto en modo avión, sobrevivió a `force-stop`, y al recuperar red dejó exactamente una fila con su `client_request_id` y una fila de adjunto. Pendientes antes del piloto: caché de la lista de obras para arranque frío offline, visibilidad/reintento de operaciones del outbox en estado `error`, y decidir el uso de `project_code_catalog`/`project_rules`. Informe: `PHASE_3_DEVICE_E2E_REPORT_2026-07-31.md`. |
| **4 — Migración y compatibilidad Excel** | Incorporar datos sin copiar errores | Sin cambios. |
| **5 — Seguridad, pruebas y piloto** | Revisión y firma | Foco añadido: pruebas de fuga entre obras (multi-tenant), por la intención de venta a terceros (enmienda 3). Piloto en dos pasos: Erick solo, luego equipo con permiso escrito. |
| **6 — Instrumentos y campañas** | Disciplinas geotécnicas | Orden: piezómetro/inclinómetro primero, reemplazando el blob genérico de lectura (§7) por estructura propia. Fisurómetro/extensómetro/clinómetro después, no ahora. |
| **7 — Integraciones** | Conectar con plataformas de cálculo | Sin cambios. |

**Aviso de calendario:** con 5–10 h/semana declaradas, el objetivo "Fases 0→3 en un trimestre" (enmienda 3) puede apretarse al haber ampliado la Fase 0. No se ajusta la cifra hoy; se revisa al cerrar el inventario de Fase 0.

**Herramienta recomendada para ejecutar (2026-07-26):** Claude Code para el trabajo de código (Fase 0 en adelante) — el repo ya usa la convención `AGENTS.md`/`CLAUDE.md` con imports, y las fases dependen de herramientas locales (adb, emulador, Expo) que un entorno Cowork no tiene de forma directa. Cowork para auditoría, decisiones de producto/IP y mantener este archivo al día entre sesiones. Regla que no cambia (enmienda 4): un solo agente escribiendo en una rama a la vez.

**Decisión de Erick (26-07-2026): usar ambos, Claude Code y Cowork, para hacer commits.** Aceptado, con protocolo obligatorio en `AGENTS.md` ("Reglas permanentes para cualquier agente") y registro de sesiones en §9 más abajo, para que nunca escriban a la vez sobre la misma rama.

**Estado del acceso de Cowork a GitHub (verificado 26-07-2026):** sin acceso funcional todavía. `git push --dry-run` falla por falta de credenciales en el entorno aislado de Cowork; no hay conector de GitHub conectado en el registro de MCP. Para habilitarlo: generar un token de acceso personal de GitHub de tipo *fine-grained*, limitado a este único repositorio (`Erickconestilo/La-Libreta-del-Peon`), con permiso de contenido lectura/escritura y expiración corta (30–90 días); no hace falta todavía porque la Fase 0 no genera commits.

---

## 9. Registro de sesiones (control de escritura entre agentes)

Regla: antes de modificar archivos o commitear, añadir una fila aquí con estado `abierto`. Al terminar, cambiar a `cerrado`. Si hay una fila `abierto` de otro agente sobre la misma rama, no escribir — avisar a Erick primero.

| Fecha | Agente | Rama | Tarea | Estado |
|---|---|---|---|---|
| 2026-07-26 | Cowork | — | Auditoría, adenda, evaluación, corrección TopoTask, inventario inicial Supabase, plan de fases consolidado | cerrado (solo lectura/documentación, sin commits) |
| 2026-07-26 | Claude Code | main | Fase 0: inventario completo de 8 migraciones no versionadas, 23 tablas Supabase, reconciliación 6 commits locales, análisis de riesgo y recomendaciones (sin aplicar nada) | cerrado |
| 2026-07-26 | Cowork | — | Segunda lectura del informe de Fase 0 de Claude Code: verificación de conteos reales (COUNT(*)), corrección de §3 (había datos, no cero), corrección de una cita errónea sobre §8 | cerrado (solo lectura) |
| 2026-07-26 | Claude Code | phase-1-domain-contract | Fase 1: ADR formalizando modelo obras/campanas/faenas, migración 015_obras_baseline.sql (retroactiva), recomendación obras/projects y profiles/users | cerrado |
| 2026-07-26 | Cowork | phase-2-fix-mocks | Arreglar mock de expo-sqlite en jest.setup.js para que los 21 tests fallidos de outbox.test.ts/sync-engine.test.ts pasen con un mock que simule estado real, no valores fijos. Excepción puntual a la regla 2 (reparto por área): tarea de solo Jest/Node, sin dispositivo ni emulador. | cerrado (23/25 verde; 2 fallos son bugs reales de producción, no del mock — ver §12) |
| 2026-07-26 | Claude Code | phase-2-cleanup | Limpieza de Fase 1 (mal etiquetada como "Fase 2"): migración 016 (unificar profiles→users), migración 017 (DROP tablas TopoTask) | cerrado |
| 2026-07-26 | Claude Code | phase-2-offline-engine | Fase 2 PARCIAL: PARTE 1 tipos TS obras/campanas/faenas (✅), PARTE 2 estructura base motor offline (test runner, SQLite schema, outbox API, diseño) — slice vertical pendiente | cerrado |
| 2026-07-26 | Claude Code | phase-2-offline-engine | Fase 2 CIERRE: verificar npm install/test, sync engine (flush/retry/conectividad), slice vertical 1 (station message por outbox), tests persistencia/sync/idempotencia | cerrado (parcial) |
| 2026-07-28 | Cowork | phase-2-fix-mocks | Arreglar los 2 bugs reales encontrados al validar el mock: (1) parseo de fecha como hora local en sync-engine.ts, (2) desempate de orden en outbox.ts. Erick autorizó explícitamente que siga Cowork ("sigue tu"), excepción puntual a regla 2, entrega luego a Codex para continuar con la app. | cerrado (25/25 tests verdes) |
| 2026-07-28 | Cowork | — | Diseño de ingeniería completo para clientRequestId en backend (migración 018, validación, modelo con INSERT ON CONFLICT idempotente, controller) — solo diseño y documentación, sin escribir código de apps/backend. Implementación queda para Claude Code. | cerrado (solo diseño/documentación) |
| 2026-07-28 | Cowork | phase-2-backend-idempotency | Implementar el diseño de clientRequestId en apps/backend (migración 018 como archivo, shared/types.ts, validación, modelo, controller, test de validación). Erick autorizó explícitamente porque Claude Code no le abre en su equipo ahora mismo. Excepción puntual a regla 2 (backend es normalmente pista de Claude Code). NO se aplica la migración a Supabase sin autorización explícita aparte — solo se crea el archivo. | cerrado (23/23 tests backend verdes, tsc sin errores; migración NO aplicada a Supabase) |
| 2026-07-29 | Cowork | — | Aplicar la migración 018 (client_request_id) al proyecto Supabase "topofield" (tmlexrsnxpmykbpeebri), autorizado explícitamente por Erick ("aplícala"). Verificado antes de tocar producción: proyecto correcto (no "topotask-backend"), columna no existía todavía. | cerrado (columna + índice único parcial creados, 3 filas existentes intactas con NULL, advisors sin hallazgos nuevos, registrado en schema_migrations) |
| 2026-07-29 | Codex | phase-2-device-e2e | Prueba end-to-end del motor offline en Galaxy real: encolar station_messages sin red, sincronización automática, reinicio de app, verificación de idempotencia y client_request_id en Supabase | cerrado (NO EJECUTADA: el preflight TypeScript móvil falló antes del build; Render seguía en 2fd2eb2 sin el backend idempotente y ADB mostraba el Galaxy como unauthorized; no se corrigió código) |
| 2026-07-29 | Cowork | — | Verificación independiente del informe final de Codex (commit `2e1e94a`, rama `phase-2-device-e2e`) antes de aceptar la Fase 2 como cerrada — no se aceptó el resumen sin comprobar. | cerrado (ver detalle en §12) |
| 2026-07-29 | Codex | phase-2-device-e2e | Corrección autorizada por Erick de los errores de integración TypeScript y continuación de la prueba E2E con backend local y Supabase real, sin despliegue ni EAS | cerrado (pausado por Erick tras verificar arreglos y antes de instalar APK; el primer assembleDebug falló al ejecutar clean sobre artefactos CMake release antiguos; backends locales y adb reverse detenidos) |
| 2026-07-29 | Codex | phase-2-device-e2e | Reanudación de la prueba E2E offline en Galaxy: regenerar Android limpio, instalar build local, ejecutar reconexión y reinicio, verificar SQLite/Supabase/logs | cerrado (BLOQUEADA en el primer envío offline: Hermes no expone `crypto`, `crypto.randomUUID()` lanzó `Property 'crypto' doesn't exist`; outbox SQLite y consulta Supabase quedaron vacíos; no se corrigió por la regla de avisar antes de arreglar un bug real) |
| 2026-07-29 | Codex | phase-2-device-e2e | Corrección autorizada del UUID runtime con `expo-crypto`, regresión, recompilación local y repetición completa de los dos escenarios E2E en Galaxy; generar informe final | cerrado (28/28 móvil, 23/23 backend, bundle Android OK; 3 pruebas reales sincronizadas una sola vez en Supabase, incluida reapertura sin backend accesible; informe E2E versionado) |
| 2026-07-29 | Cowork | main | Integrar `phase-2-device-e2e` a `main` (fast-forward, 12 commits) antes de empezar Fase 3. Ramas ya fusionadas (`phase-2-fix-mocks`, `phase-2-backend-idempotency`, `phase-2-device-e2e`) borradas por estar subsumidas. | cerrado (main en `88b3427`, 14 commits por delante de `origin/main`, sin push) |
| 2026-07-29 | Cowork | main | Antes de escribir código de Fase 3, verificar que las tablas `obras/*` existen de verdad en el proyecto Supabase que usa `apps/backend` — descubrimiento de que ADR 001 se equivocó de proyecto (ver corrección en el ADR y en §1 de la tabla de fases arriba). | cerrado (hallazgo documentado, decisión de Erick: redecidir el modelo desde cero, sin migrar `obras`) |
| 2026-07-30 | Cowork | main | Erick decide adoptar el diseño de `deprecated/014_monitoring_rounds.sql` para Fase 3 ("tomo tu mejor opción, sigamos"). Reactivar como migración 019 (sin `total_station`, ya cubierto por `prism_monitoring_observations`) y completar el backend que faltaba: crear/listar rondas, detalle de ronda con puntos, CRUD de puntos de control, umbrales, histórico de lecturas. | cerrado (migración 019 creada, NO aplicada a Supabase — pendiente autorización explícita; 27/27 tests backend, `tsc` limpio; ver §12) |
| 2026-07-30 | Cowork | — | Aplicar la migración 019 (monitoring_rounds) al proyecto Supabase "topofield", autorizado explícitamente por Erick ("aplícala"). Verificado antes: proyecto correcto, ninguna de las 9 tablas existía todavía, `projects`/`users` (destino de las FK) ya poblados. | cerrado (9 tablas creadas, catálogo `instrument_types` sembrado con 6 filas, migración registrada en `schema_migrations`. **Hallazgo de seguridad: las 9 tablas nuevas quedaron con RLS desactivado**, a diferencia de las 37 tablas restantes del proyecto — advisor de Supabase lo marca `critical`. No se aplicó el remedio sin decidir antes las políticas; ver detalle en §12) |
| 2026-07-30 | Cowork | main | Erick pide, además de diseñar las políticas RLS, revisar y corregir la documentación del repo que va "hacia adelante y hacia atrás" (contradicciones entre docs de fases distintas). Añadir `.gitattributes`, corregir `NEXT_CHAT_HANDOFF.md`, banners de corrección en `FASE_0_INVENTARIO_COMPLETO.md`/`FASE_0_INVENTARIO_RAW.txt`/`FASE_1_RECOMENDACIONES.md`, revisar `LAUNCH_PLAN.md`/`PRODUCT_STRATEGY.md`/`UX_RESEARCH_PLAN.md`, diseñar migración 020 (RLS) sin aplicarla. | cerrado (ver detalle en §12; migración 020 creada y NO aplicada — pendiente confirmación explícita) |
| 2026-07-30 | Cowork | — | Aplicar la migración 020 (RLS en las 9 tablas de monitoring_rounds) al proyecto Supabase "topofield", autorizado explícitamente por Erick ("APLICAR"). | cerrado (`apply_migration` devolvió éxito; `list_tables` confirma `rls_enabled: true` en las 24 tablas de `public`; `get_advisors` ya no muestra el hallazgo `rls_disabled critical`, solo queda el WARN preexistente de `auth_leaked_password_protection`, sin relación) |
| 2026-07-31 | Codex | codex/phase-3-mobile-monitoring | Fase 3 móvil: pantallas de rondas de auscultación, puntos de control, lecturas e histórico. Decisión previa: las lecturas usan SQLite/outbox/sync-engine y `clientRequestId`, siguiendo el patrón E2E de Fase 2; no se modifica backend ni Supabase. | cerrado (TypeScript limpio, Jest móvil 30/30 y export Android OK; QA contra la app pública bloqueada porque Render devuelve 404 en rutas Fase 3; foto pendiente de endpoint de adjuntos) |
| 2026-07-31 | Cowork | main | Verificar informe de Codex (git real, coautoría, diff, ausencia de endpoint `reading_attachments`, nada empujado) y fusionar `codex/phase-3-mobile-monitoring` a `main`. | cerrado (fast-forward limpio, `7af2936`, rama borrada) |
| 2026-07-31 | Codex | codex/phase-3-reading-attachments | Publicar el `main` local que contiene Fase 3 en Render, añadir el endpoint de adjuntos de lectura, conectar la captura móvil y validar el flujo en Galaxy sin aplicar migraciones. | cerrado parcialmente: endpoint, UI, outbox y pruebas listos en `96b7f2d`; GitHub `main` publicado pero Render sigue en `2fd2eb2` sin acceso al redeploy; Galaxy no aparece en ADB, por lo que el E2E físico queda pendiente. |
| 2026-07-31 | Codex | codex/render-deploy-evidence | Registrar la comprobación posterior al push de `main` y cerrar la evidencia de despliegue de Render. | cerrado (tres comprobaciones tras publicar `149ad67`: Render sigue en `2fd2eb2` y las rondas devuelven `404`; no hay hook GitHub convencional visible y no hay credencial de Render disponible localmente). |
| 2026-07-31 | Codex | codex/render-backend-lockfile | Corregir el bloqueo de dependencias que impide el build de Render: `apps/backend/package.json` requiere esbuild 0.28.1 mientras su lock local conserva 0.28.0. | cerrado (`npm ci` y `npm ci --omit=dev` con npm 10.9.8 pasan; build y 30/30 tests backend verdes) |
| 2026-07-31 | Codex | codex/phase-3-galaxy-e2e-report | Prueba E2E de Fase 3 en Galaxy: ronda, lectura con foto, repetición offline tras reinicio, idempotencia en Render/Supabase; documentación e informe final. | cerrado (escenario limpio offline superado; ver informe de dispositivo y pendientes UX en §12) |
| 2026-07-31 | Codex | codex/pilot-blockers-mobile | Resolver pendientes prioritarios: diagnóstico de sesión, caché offline de Obras, diagnóstico/reintento de outbox, decisión de catálogo/reglas y baseline RLS legacy. | cerrado (app, decisión y baseline documental completos; queda solo confirmación opcional de configuración Auth por Erick) |
| 2026-07-31 | Codex | codex/release-readiness-catalog | Bloque 0 catálogo genérico, Bloque A dependencias/audit, Bloque B firma AAB local y Bloque C inventario de historial sin reescritura. | cerrado (commits separados; AAB firmado y verificado, instalación Galaxy pendiente porque ADB no detectó dispositivo; purga bloqueada a la espera de confirmación explícita de cadenas) |
| 2026-07-31 | Codex | codex/release-readiness-catalog | Bloque C autorizado: generar esta copia saneada desde el backup mirror original mediante una sola reescritura de las nueve cadenas confirmadas, sin push forzado. | cerrado (nueve búsquedas literales a cero, `git fsck --full` correcto y sin remoto) |
| 2026-08-01 | Codex | codex/release-readiness-catalog | Sustituir el worktree original por esta copia saneada, guardar y reaplicar el diff local pendiente de `apps/mobile/package.json`, y restaurar solo la configuración de `origin`, sin sincronizar ni publicar. | cerrado (backup bare íntegro, worktree previo preservado, parche reaplicado limpio; backend compila y 34/34 tests, móvil TypeScript limpio y 35/35 tests) |
| 2026-08-01 | Codex | codex/phase-5-multitenant-security | Fase 5: auditar y reforzar el aislamiento multi-tenant en todos los endpoints, añadir regresiones entre obras, revisar advisor de Supabase en modo lectura y preparar checklist de piloto. | cerrado (incidencias y flujos de foto reforzados; backend 40/40, móvil 35/35; queda una decisión de producto sobre visitante y auscultación) |
| 2026-08-02 | Codex | codex/phase-5-multitenant-security | Limpiar referencias locales residuales tras la purga de historial: validar ocho ramas ya fusionadas, eliminar la referencia remota obsoleta y podar objetos inalcanzables, sin contactar con origin. | cerrado (ocho ramas y `refs/remotes/origin/main` eliminadas; las nueve cadenas autorizadas dan 0 coincidencias exactas y `git fsck --full` sin errores) |

---

## 10. Segunda lectura del informe de Fase 0 (Cowork, 26-07-2026)

**Calidad del informe: buena.** Estructura correcta, separa commits seguros de bloqueados con motivo, y los tres documentos que dice haber entregado (`FASE_0_INVENTARIO_COMPLETO.md`, `FASE_0_INVENTARIO_RAW.txt`, `apps/backend/src/scripts/inventory-supabase-schema.ts`) existen de verdad con contenido, verificado por tamaño de archivo — no es un resumen inventado. Siguió el protocolo de §9 correctamente (registró y cerró su fila).

**Corrección aplicada:** los conteos de filas del informe eran correctos; la memoria de Cowork decía lo contrario y ya se corrigió en §3.

**Corrección a una afirmación del informe:** dice que la "Opción C" (mantener temporalmente el modelo `obras` y la migración 014 en paralelo) está "prohibida por el plan maestro §8". Revisado el documento original: no es así. La única frase sobre "no en paralelo" pertenece al §7 (Fase de integraciones con fabricantes, no a modelos de datos internos). El propio §8, para el problema equivalente de `Station`, recomienda explícitamente la convivencia temporal deliberada ("introducir entidades nuevas junto al modelo legado... retirar el significado ambiguo al final"), no la prohíbe.

**Recomendación de Cowork para la decisión de Fase 1** (input, no decisión final — la decisión formal es de Erick vía ADR):

Con los datos reales ahora confirmados, la familia "obras" deja de ser un experimento descartable: es 76 semanas-obra de historial ya poblado (72 sensores, 145 mediciones). Adaptarla como base del modelo de faenas es probablemente menos trabajo y menos riesgo que migrar ese historial real hacia una tabla nueva. La migración 014 (monitoring_rounds) tiene 0 datos y usa el blob genérico de lectura ya señalado como antipatrón en §7 de este archivo — no aporta ninguna ventaja sobre adaptar lo que ya funciona.

`total_station_*` y `leveling_*` no son parte de esta decisión: resuelven un problema distinto (campañas de instrumento, Fase 6), no el de faenas semanales (Fase 3), y ambos se conservan de forma independiente.

**Decisión formal de Erick (26-07-2026): adaptar la familia "obras" como base del modelo de faenas.** Migración 014 (monitoring_rounds) no se aplica — queda descartada para este propósito, no se migra el historial real hacia ella. `total_station_*`/`leveling_*` se conservan sin cambios, resuelven Fase 6.

**Siguiente paso concreto para Fase 1:** redactar el ADR de dominio (§1 de la adenda) formalizando `obras`→`campanas`→`jornadas`→`sensores`→`mediciones`→`estacionamientos` como el contrato de faenas, documentar sus columnas reales vía migración retroactiva (`015_obras_baseline.sql`, sin aplicar — ya existe en Supabase), y decidir el nombrado/ajustes mínimos necesarios (p. ej. si `obras` se unifica o convive con `projects`, y si `profiles` se unifica con `users`). Las 4 tablas de integración TopoTask (telegram, correo) quedan descartadas, confirmando la recomendación del informe de Fase 0.

---

## 11. Fase 2 aplicada — verificación de Cowork (26-07-2026)

**Resultado: correcto, con una afirmación falsa en el reporte que se corrige aquí.**

Verificado directo contra Supabase tras la ejecución de Claude Code (migraciones 016 y 017):

- `profiles` eliminada, 0 referencias en `information_schema`. `incidencias`, `incidencia_fotos`, `obra_destinatarios`, `envios_correo` eliminadas — confirmado, no solo reportado.
- `users`: 15 filas reales (antes 4). Datos consistentes: 10 filas con `legacy_usuario_id` 1–10 sin huecos ni duplicados, más 1 fila migrada (`remoteonly-...@topotask.local`) sin ID legacy porque nunca lo tuvo en origen — el reporte decía "11 migradas", que es correcto; la aparente discrepancia con "10" se explica y no es un fallo.
- Commits `77508b9`, `9c42512`, `87565fb` reales, sincronizados con `origin/main` (0 de diferencia).

**Corrección importante:** el reporte afirmó que `backups_topofield/backup_manifest_app_movil_2026-07-26.json` era "válido para rollback". Es falso — la nota interna del propio archivo dice explícitamente que **no** exporta filas y que su objetivo es solo comparar checksums, no restaurar. Además cubre las tablas nativas de TopoField, no `profiles`. El otro respaldo (`backup_topotask_tables_2026-07-26.json`) sí tiene datos fila por fila, pero tampoco incluye `profiles` — solo las 4 tablas TopoTask (que estaban vacías, así que ahí no hubo pérdida real). Ambos respaldos son de las 07:51 de esta mañana, de un trabajo de separación TopoTask anterior a esta sesión — **no se generó ningún respaldo nuevo específico para esta migración**, pese a que se pidió explícitamente en el encargo.

**Consecuencia:** las 11 filas reales de `profiles` se migraron sin un respaldo restaurable propio. No hay nada que corregir retroactivamente (la tabla ya no existe y el estado final se ve correcto por inspección directa), pero no se puede afirmar con certeza absoluta que no hay ninguna fila con un dato mal mapeado, más allá de lo que la inspección directa ya descartó (sin duplicados, sin huecos en la secuencia).

**Lección para próximas migraciones destructivas (aplica a Fase 3 en adelante):** exigir un respaldo fila-por-fila generado en el momento de la tabla específica que se va a modificar o eliminar, no aceptar la reutilización de un respaldo preexistente de otro propósito sin verificar que cubre exactamente las tablas afectadas.

---

## 12a. Lista consolidada de pendientes (31-07-2026)

Erick pidió juntar en un solo lugar todo lo que sigue pendiente en el repo (estaba disperso en varias secciones y documentos). Esta lista sustituye a esas menciones sueltas para efectos de priorización; si hay contradicción, manda esta.

**Ahora (bloquea piloto real o es fricción activa):**
- **Corregido en app (31-07-2026):** re-login tras inactividad. El refresh se hacía, pero un fallo transitorio borraba el access token persistido. Ahora se conserva y solo se invalida ante `401 INVALID_REFRESH_TOKEN`. Pendiente de Erick, solo como comprobación de dashboard: confirmar que Auth no tenga activados *Inactivity timeout*, *Time-box user sessions* o sesión única con una política que quiera mantener.
- **Corregido en app (31-07-2026):** caché de Obras por sesión local en SQLite (`project_list_cache`, migración local 002), con aviso de datos sin actualizar si no hay red.
- **Corregido en app (31-07-2026):** Perfil lista elementos de outbox en `error`, muestra motivo/intentos y permite reintento manual inmediato.
- **Decidido (31-07-2026):** mantener `project_code_catalog` y `project_rules`. El supuesto catálogo alternativo no existe: `projects.routes.ts` expone precisamente `project_code_catalog`; `project_rules` ya gobierna `auto_confirm_green` en backend. No duplicar tablas ni endpoints.
- **Documentado (31-07-2026):** baseline RLS en `apps/backend/migrations/baseline/legacy_rls_baseline.sql`, solo referencia y no aplicable. La consulta real a `pg_policies` devuelve 24 tablas con `legacy deny all`, no 37; queda corregir la cifra histórica en una auditoría futura, sin inventar políticas inexistentes.

**Después (importante, no bloquea uso real):**
- Catálogo semilla genérico para obras nuevas, sin datos operativos reales.
- Instalar en Galaxy el AAB release local ya firmado cuando ADB vuelva a detectar el dispositivo; el AAB y su APK universal se verificaron criptográficamente.
- Catálogo semilla genérico/real del producto (Fase 1, enmienda 5).
- Fase 6: reemplazar el blob genérico de `instrument_readings` por estructura propia cuando lleguen piezómetro/inclinómetro.

**Aparcado (sin retorno claro ahora):**
- Purga de historial git de nombres reales de obra: el worktree real fue sustituido por la copia saneada el 01-08-2026; solo queda autorización explícita posterior para cualquier `push --force` tras avisar a colaboradores.
- Fisurómetro/extensómetro/clinómetro (después de piezómetro/inclinómetro).
- Acceso GitHub de Cowork vía token fine-grained (no bloquea nada hoy).

**Acción de Erick, no delegable a un agente:** permiso por escrito del empleador que distinga "usar TopoField en obra" de "ser propietario del código" (ver §5).

## 12. Bitácora de avances (una línea por hito, con contexto)

Regla (26-07-2026): cada avance real —fase completada, decisión tomada, corrección aplicada, hallazgo importante— se añade aquí en el momento, con una frase corta que dé idea y contexto. No sustituye las secciones detalladas de arriba; es el resumen rápido para no tener que leer todo el archivo.

- **2026-08-02 — Limpieza de referencias tras purga (Codex):** confirmadas ocho ramas obsoletas sin commits exclusivos frente a Fase 5 y eliminadas junto a `refs/remotes/origin/main`; reflogs expirados y objetos inalcanzables podados sin fetch, pull ni push. El `HEAD` del bare saneado, que aún apuntaba a una rama borrada, quedó normalizado a `main`. Las nueve cadenas autorizadas devuelven 0 coincidencias exactas en `git log --all -p`; solo permanecen `main` y la rama de Fase 5, y `git fsck --full` no muestra errores. Se detectaron variantes históricas en minúscula fuera de la lista explícitamente autorizada; no se reescribieron y requieren una autorización nueva si se desea purgarlas.
- **2026-08-01 — Fase 5, aislamiento multi-tenant (Codex):** auditoría de controladores/modelos/rutas completada. Incidencias ahora valida estación/prisma bajo scope dentro de la transacción y un topógrafo no puede crear una incidencia sin recurso de obra; los cinco flujos de foto autorizan el recurso antes de consultar Storage. Pruebas de dos topógrafos y dos obras, backend 40/40 y móvil 35/35 en verde. El advisor de Supabase no muestra RLS/políticas nuevas; conserva solo el WARN conocido de protección de contraseñas filtradas. Documentos: `MULTI_TENANT_SCOPE_AUDIT_2026-08-01.md` y `PILOT_READINESS_CHECKLIST.md`. Queda confirmar la política de visitante para lecturas de auscultación.
- **2026-07-31 — Purga local final de historial autorizada (Codex, Bloque C):** esta copia se reconstruyó desde el backup mirror original y recibió una única reescritura con las nueve cadenas autorizadas. Las nueve verificaciones literales sobre `git log -p --all` devuelven 0; `git fsck --full` termina correctamente. No conserva remoto GitHub y no se ejecutó ningún push.
- **2026-08-01 — Worktree saneado activado (Codex, Bloque C):** el repositorio de trabajo `C:\Users\guill\Documents\Aplicacion_Movil\topofield` ya apunta a la copia saneada; `origin` fue reconfigurado sin fetch/pull/push. El backup bare permanece en `C:\Users\guill\Documents\Aplicacion_Movil\topofield-backup-before-history-purge-20260731.git`, el árbol previo se preservó en `C:\Users\guill\Documents\Aplicacion_Movil\topofield-pre-sanitized-replacement-20260801`, y el parche externo reaplicado queda en `C:\Users\guill\.topofield\pending-package-json-before-history-replacement-20260801.diff`. Tras `npm ci`, backend compila y pasa 34/34 tests; móvil pasa TypeScript y 35/35 tests. No hubo fetch, pull ni push.
- **2026-07-31 — Inventario de purga de historial (Codex, Bloque C):** búsqueda de solo lectura completada sobre los 141 commits accesibles, incluyendo `git log -G`, `git log -S` y `git grep` por commit. Hay nombres reales de obra y referencias operativas en migraciones, datos de estaciones, pantallas y documentación histórica. No se creó mirror, no se usó `filter-repo` ni se hizo ningún push forzado: queda una compuerta explícita de confirmación de Erick antes de cualquier reescritura.
- **2026-07-31 — Firma release local y AAB (Codex, Bloque B):** generado el keystore local de TopoField fuera del repo y el configurador versionado que lo conecta a `release` tras cada prebuild. Se generó `app-release.aab` (40.059.801 bytes), `jarsigner` devolvió `jar verified` y el APK universal de Bundletool tiene la misma huella SHA-256 del certificado. `adb devices` no mostró ningún dispositivo, así que la instalación física queda pendiente; no se subió nada a Play Store.
- **2026-07-31 — Dependencias y auditoría (Codex, Bloque A):** `expo-network` quedó en `~56.0.5`, `react-native-maps` en `1.27.2` y `react-native-screens` en `~4.26.0`; `npx expo install --check` confirma dependencias compatibles. La auditoría actual de `apps/backend` devuelve 0 vulnerabilidades tanto con dependencias de desarrollo como con `--omit=dev`; las 11 alertas moderadas anotadas antes eran estado histórico, así que no hubo `npm audit fix` que aplicar.
- **2026-07-31 — Catálogo semilla genérico (Codex, Bloque 0):** al crear una obra se insertan, dentro de la misma transacción, cuatro códigos neutros de ejemplo (`EJ-*`), dos puntos de control de muestra (nivel digital y piezómetro) y umbrales indicativos. Cada registro se marca como dato de muestra para sustituir antes de una campaña; no contiene nombres, códigos ni proceso de ningún cliente o empleador.
- **2026-07-26 — Auditoría inicial (Cowork):** plan maestro de rework aprobado con 5 enmiendas; modelo de faenas reducido de 10 a 5 tablas por presupuesto de tiempo real (5-10h/semana).
- **2026-07-26 — Corrección TopoTask:** no es contaminación, son dos proyectos Supabase separados; el único vínculo es una importación de datos históricos legítima de mayo.
- **2026-07-26 — Fase 0 (Claude Code):** reveló deriva de esquema masiva — 8 migraciones y 23 tablas ya construidas en Supabase sin archivo local, incluida una familia "obras" con datos reales (72 sensores, 145 mediciones).
- **2026-07-26 — Corrección de conteos:** Cowork había dicho "0 filas, sin riesgo" basado en una estimación de Postgres poco fiable; los datos reales existían desde el principio.
- **2026-07-26 — Decisión Fase 1:** adoptar la familia "obras" como modelo de faenas; migración 014 (monitoring_rounds) descartada por tener 0 datos y usar un blob de lectura genérico.
- **2026-07-26 — Fase 1 fusionada a main:** ADR 001 + migración 015 documental + `deprecated/014` con explicación; incluye condición explícita de reconciliación obras/projects (no convivencia indefinida).
- **2026-07-26 — Fase 2 aplicada a producción:** `profiles` (11 filas) unificada en `users`; 4 tablas TopoTask (0 filas) eliminadas. Primer cambio destructivo real del rework.
- **2026-07-26 — Corrección de respaldo:** el respaldo citado como "válido para rollback" en Fase 2 no lo era (solo checksums, no cubría `profiles`); no hubo pérdida de datos pero fue suerte, no proceso. Regla 9 añadida a `AGENTS.md`.
- **2026-07-26 — Limpieza de carpeta raíz:** 274 capturas de depuración obsoletas (68 MB) archivadas y luego eliminadas de `Aplicacion_Movil/`, sin tocar `topofield/`.
- **2026-07-26 — Tres agentes coordinados:** Codex sumado (track móvil/builds), Claude Code (backend/dominio), Cowork (auditoría/memoria); protocolo de un solo agente por rama en `AGENTS.md`.
- **2026-07-26 — Corrección de numeración de fases:** la limpieza de `profiles`/TopoTask se llamó "Fase 2" pero en realidad era limpieza dentro de Fase 1. La Fase 2 real (motor offline: SQLite, outbox, estados de sincronización) no se ha empezado. Fase 3 (pantallas de faenas) queda bloqueada hasta cerrarla — construir faenas sin motor offline no cumpliría la promesa central de la app (funcionar sin cobertura).
- **2026-07-26 — Fase 2 iniciada (PARCIAL):** PARTE 1 completa (tipos TS obras/campanas/faenas en `shared/obras-types.ts`). PARTE 2 estructura base: test runner (Jest), SQLite schema (migración 001, tabla outbox), outbox API, diseño documentado en `lib/offline/DESIGN.md`. Pendiente: sync engine, slice vertical completo (Station Message), tests. Fase 2 no cerrada, Fase 3 sigue bloqueada.
- **2026-07-26 — Fase 2 CIERRE PARCIAL (Claude Code):** npm test confirmado funcionando sin errores de binario. Sync engine implementado (flush/retry/backoff/conectividad), outbox API completo, slice vertical 1 (Station Message) integrado en hook. Tests escritos (21 fallan por mock de expo-sqlite simplificado, 4 pasan). Pendiente: mejorar mocks para que tests pasen, testing end-to-end en dispositivo, backend sin soporte clientRequestId todavía. Fase 2 no cumple criterio completo de salida, Fase 3 sigue bloqueada hasta resolver.
- **2026-07-28 — Mock de expo-sqlite arreglado (Cowork, rama `phase-2-fix-mocks`):** sustituido el mock de valores fijos por uno respaldado por `node:sqlite` (SQLite real embebido en Node 22, in-memory), que aplica CHECK/UNIQUE/datetime igual que producción. Resultado: 23/25 tests pasan. Los 2 que fallan son bugs reales, no defectos del mock — no se tocó `sync-engine.ts` ni `outbox.ts`: (1) `syncItem()` en `sync-engine.ts` parsea `last_sync_attempt_at` (string UTC que devuelve `datetime('now')` de SQLite) con `new Date(...)`, que en JS interpreta ese formato sin zona como hora **local**; en un huso distinto de UTC (ej. Europe/Madrid, UTC+2 en verano) el backoff exponencial queda roto — cree que ya pasó el tiempo de espera cuando no es así. Afectaría también en dispositivo real, no solo en tests. (2) `getPending()`/`getErrors()`/`getConflicts()` ordenan solo por `created_at` (precisión de 1 segundo); si dos items se encolan dentro del mismo segundo, el orden no está garantizado — falta un desempate (autoincrement o timestamp de mayor precisión). Pendiente decisión de Erick: quién arregla estos dos (por reparto de pistas le tocaría a Codex/apps-mobile, o excepción puntual como esta).
- **2026-07-28 — Los 2 bugs reales corregidos (Cowork, rama `phase-2-fix-mocks`):** Erick autorizó explícitamente seguir ("sigue tu"). (1) `sync-engine.ts`: nueva función `parseSqliteUtcDate()` fuerza el parseo del timestamp de SQLite como UTC (antes `new Date()` lo interpretaba como hora local, rompiendo el backoff fuera de UTC). (2) `outbox.ts`: `getPending/getErrors/getConflicts` añaden `rowid` como desempate tras `created_at` (que solo tiene precisión de 1 segundo), usando la columna implícita de SQLite que crece monótonamente con cada INSERT. **25/25 tests pasan.** Fase 2 queda con la suite de tests en verde; sigue pendiente el soporte de `clientRequestId` en el backend y la prueba end-to-end en dispositivo real antes de considerar Fase 2 cerrada del todo. Erick decidió que a partir de aquí retoma Codex para seguir con la app.
- **2026-07-28 — clientRequestId implementado en backend (Cowork, rama `phase-2-backend-idempotency`):** Erick pidió que lo hiciera Cowork porque Claude Code no le abre en su equipo en este momento — excepción puntual a la regla 2 (backend es normalmente pista de Claude Code). Migración `018_station_messages_client_request_id.sql` creada como archivo (columna `client_request_id UUID` + índice único parcial `WHERE client_request_id IS NOT NULL`) — **no aplicada a Supabase**, requiere autorización explícita aparte (regla 6). `shared/types.ts`, validación (zod), `station-messages.model.ts` (INSERT con `ON CONFLICT ... DO NOTHING RETURNING id`; si no inserta, busca y devuelve el mensaje ya existente por `client_request_id` sin duplicar el change-log) y el controller actualizados. Test de validación nuevo (`station-messages-validation.test.ts`, 3 casos) añadido a `all.test.ts`. Verificado con `tsc` (sin errores) y `node --test`: **23/23 tests del backend pasan** (build y test corridos desde una copia local en disco rápido, no sobre el montaje de red, por lentitud de este último — mismo método que en Fase 2 móvil). No hay test de integración real contra Postgres para el `ON CONFLICT` porque el backend no tiene infraestructura de tests contra base de datos real (los tests existentes son todos de lógica pura); esa parte concreta solo queda verificada por revisión de código y por `tsc`, pendiente de confirmarse con datos reales cuando Codex haga la prueba en dispositivo o cuando se aplique la migración.
- **2026-07-29 — Migración 018 aplicada a Supabase (Cowork):** Erick autorizó explícitamente ("aplícala"). Verificado el proyecto correcto antes de tocar nada (`topofield` / `tmlexrsnxpmykbpeebri`, no `topotask-backend`). Columna `client_request_id UUID` e índice único parcial `idx_station_messages_client_request_id` confirmados con `information_schema`/`pg_indexes`; las 3 filas existentes de `station_messages` quedaron con `client_request_id = NULL` (sin pérdida ni corrupción de datos). Registrada también en la tabla propia `schema_migrations` del backend para que `npm run migrate` no la reintente. `get_advisors` (security + performance) revisado: nada nuevo relacionado con este cambio, solo hallazgos preexistentes del proyecto (password protection, índices sin usar en otras tablas). Con esto, lo único que falta para cerrar Fase 2 del todo es la prueba end-to-end de Codex en dispositivo real.
- **2026-07-29 — Prueba E2E offline bloqueada en preflight (Codex):** la prueba en Galaxy no se ejecutó porque `npx tsc --noEmit --project apps/mobile/tsconfig.json` falló antes del build: `OfflineQueueEntityType` no admite `station_message`, `use-station-messages.ts` usa `message` aunque `CreateStationMessageInput` exige `body`, el objeto optimista no cumple `StationMessage` y los mocks de `expo-network` tienen errores de tipo. Jest móvil sí produjo literalmente `Tests: 25 passed, 25 total` y backend `pass 23 / fail 0`, demostrando que esas suites no detectan la rotura de integración. Además, Render respondió literalmente `{"commit":"2fd2eb2fab825f3d9df84dfa631d037ac0608e67","status":"ok"}`, por lo que no contiene `9c6a96e`, y `adb devices -l` mostró `R5CY21X6FLE unauthorized`. No se tocó código por la regla explícita de parar y avisar ante un bug real.
- **2026-07-29 — Preflight E2E reparado con autorización de Erick (Codex):** alineados `OfflineQueueEntityType`/`OfflineQueueStatus` con el CHECK real de SQLite, corregido el slice de station message para usar `body`, completado el objeto optimista `StationMessage`, añadido `clientRequestId` al log de encolado y tipados los mocks/callbacks de Jest. Output literal posterior: TypeScript móvil exit 0 sin salida, `Tests: 25 passed, 25 total`; backend `pass 23 / fail 0`. La sesión E2E sigue abierta hasta completar los dos escenarios en Galaxy.
- **2026-07-29 — Prueba E2E pausada a petición de Erick (Codex):** backend idempotente local confirmado con `{"commit":null,"status":"ok"}` y Galaxy autorizado por ADB. El primer build `assembleDebug` no llegó a compilar la APK porque la tarea `:app:externalNativeBuildCleanRelease` intentó limpiar artefactos CMake release antiguos con rutas mezcladas `C:\tf`/ruta larga y terminó literalmente en `BUILD FAILED in 1m 9s`; no es un fallo de TypeScript ni de los tests. Al pausar se detuvieron los listeners locales 3000/3001 y se eliminó `adb reverse`. Reanudación exacta: limpiar/regenerar Android desde `C:\tf` como indica el runbook, arrancar backend local capturado, compilar debug, instalar y ejecutar los dos escenarios.
- **2026-07-29 — APK debug E2E generada localmente (Codex):** tras reconectar el Galaxy, `npx expo prebuild --platform android --clean --no-install` regeneró Android desde `C:\tf` y `gradlew app:assembleDebug` terminó literalmente en `BUILD SUCCESSFUL in 8m 3s` (348 tareas, sin EAS). APK preparada en `C:\tf\apps\mobile\android\app\build\outputs\apk\debug\app-debug.apk`; pendiente instalar y ejecutar escenarios.
- **2026-07-29 — Verificación independiente de Cowork sobre el cierre de Fase 2 (commit `2e1e94a`):** no se aceptó el informe de Codex solo por su redacción — se comprobó por separado: (1) `git log`/`git show` del commit y de los 2 intermedios (`4a71088`, `a8d71f4`) confirman una secuencia real de bug real→pausa→autorización→fix, no un relato inventado; sin coautor. (2) Consulta directa a Supabase (`topofield`): existen exactamente 3 filas en `station_messages` con `client_request_id` no nulo, una por cada UUID citado en el informe, y una consulta de `GROUP BY ... HAVING COUNT(*) > 1` devolvió vacío — cero duplicados, confirma la idempotencia real, no solo narrada. (3) Tests y `tsc` re-ejecutados por Cowork de forma independiente (copia local en disco rápido, no fiando del pegado de Codex): `tsc --noEmit` sale limpio y Jest da **28/28** exacto, en las 4 suites (`outbox`, `sync-engine`, `sync-handlers`, `random-id`) — coincide con lo reportado. (4) Revisión de los diffs de `_layout.tsx`, `session-provider.tsx` y `sync-handlers.ts`: los cambios descritos (motor de sync arrancado una sola vez en el layout raíz, condicionado a sesión + DB lista; sesión técnica preservada solo ante error transitorio, no ante 401 real) están realmente en el código, no solo en la narrativa. (5) `git diff --ignore-space-at-eol` confirma que el resto de archivos "modificados" en el árbol de trabajo siguen siendo el mismo ruido de CRLF preexistente, nada nuevo oculto. **Conclusión: el informe de Codex se acepta como veraz.** Fase 2 completa queda cerrada técnicamente; sigue pendiente lo que el propio informe señala como no hecho (desplegar backend idempotente a producción, integrar la rama, firma estable + AAB para Play Store) antes del piloto real.
- **2026-07-29 — Bug runtime real bloquea el outbox en Galaxy (Codex):** APK debug instalada con `adb install -r` (`Success`), sesión técnica `Topógrafo` validada y modo avión aplicado con Wi-Fi/datos desactivados. Al pulsar `Dejar mensaje`, la UI mostró literalmente `Property 'crypto' doesn't exist`: `apps/mobile/hooks/use-station-messages.ts` llama dos veces a `crypto.randomUUID()`, API que Hermes no expone en esta configuración. Evidencia posterior: consulta local `select ... from outbox` devolvió `[]` y Supabase devolvió `[]` para los dos cuerpos E2E; no hubo escritura ni duplicado. Conectividad restaurada. La prueba no supera el escenario 1 y Fase 2 sigue abierta; no se corrigió este bug nuevo porque el encargo exige parar y avisar a Erick antes de arreglarlo.
- **2026-07-29 — UUID Hermes corregido (Codex):** con autorización de Erick se sustituyó el global inexistente `crypto.randomUUID()` por `expo-crypto` detrás de `createRandomId()`, con test unitario específico. El primer envío offline real pasó de no crear ninguna fila a quedar `pending` en SQLite con UUID v4 y sincronizar automáticamente.
- **2026-07-29 — Arranque global del sync corregido (Codex):** el E2E reveló que el motor nacía dentro de `useCreateStationMessage`, por lo que reabrir en `Obras` no sincronizaba hasta entrar en otra estación y podía crear intervalos repetidos. Se movió el bootstrap al layout raíz después de SQLite y de la hidratación de sesión; el manejador de outbox quedó en `lib/offline/sync-handlers.ts`. Una sesión técnica ya validada se conserva ante fallo de red transitorio, pero sigue invalidándose ante un 401 real.
- **2026-07-29 — Fase 2 offline cerrada en Galaxy (Codex):** dos escenarios solicitados y un tercero endurecido pasaron con backend local actual + Supabase real: reconexión automática, persistencia tras `force-stop`, reapertura sin túnel `adb reverse` al backend, sincronización desde `Obras`, UI final con mensajes y repetición controlada del mismo UUID. Supabase confirmó una fila por cada `client_request_id`; SQLite terminó `synced`, `retry_count=1`; 0 errores runtime de dispositivo y 0 errores 5xx/backend. Regresión final: móvil 28/28, backend 23/23, TypeScript y export Android OK.
- **2026-07-29 — Build autónoma instalada en Galaxy (Codex):** `assembleRelease` local produjo una APK de 53.051.310 bytes, instalada sobre la debug con `adb install -r` (`Success`). Play Protect preguntó si debía enviar la app a Google y se eligió `No enviar`, sin desactivar la protección. La app abrió con Metro detenido y sin `adb reverse`, quedó en `MainActivity` y `logcat` mostró 0 errores fatales/runtime. Usa firma debug: válida para seguir probando en este Galaxy, no para Play Store ni piloto distribuido.
- **2026-07-26 — Falsa alarma de Cowork sobre "git status", corregida:** Cowork vio `git status --short` = 63 y Claude Code reportó 10, y concluyó (con la primera respuesta rápida de Erick a una pregunta de opción múltiple) que la sesión de Claude Code no era confiable. **Era al revés.** Erick pegó el output literal de su propia terminal: 10 archivos, coincidiendo con Claude Code. El entorno de Cowork tenía una **vista desactualizada de los cambios de archivo sin commitear** (los commits ya hechos y los datos de Supabase sí se verificaron bien durante todo el día; el problema es específico de archivos modificados en vivo por otro proceso, sin pasar por commit). **Corrección de proceso:** de aquí en adelante, para verificar `git status` de cambios *sin commitear*, confiar en el output que pegue Erick desde su propia terminal o el que reporte el agente, no en el bash de Cowork — Cowork sigue siendo fiable para commits ya hechos, contenido de archivos committeados, y consultas a Supabase.
- **2026-07-29 — `phase-2-device-e2e` integrada a `main` (Cowork):** fast-forward limpio, 12 commits, sin conflictos. Ramas ya subsumidas (`phase-2-fix-mocks`, `phase-2-backend-idempotency`, `phase-2-device-e2e`) borradas. `main` local queda 14 commits por delante de `origin/main`, sin push todavía (no autorizado).
- **2026-07-29 — ADR 001 SUPERSEDIDO — error de proyecto Supabase descubierto (Cowork):** al preparar el código de Fase 3, verificación directa con `list_tables`/`information_schema` reveló que `obras/campanas/jornadas/sensores/mediciones/estacionamientos` (con las mismas 6/7/0/72/145/22 filas del ADR 001) **no están en `topofield`** (`tmlexrsnxpmykbpeebri`, el proyecto que usa de verdad `apps/backend/.env`) **sino en `topotask-backend`** (`jwckuoiossieiyankkvh`), que además tiene `organizations`/`portfolios`/`asset_nodes` — parece ser la base de datos real de TopoTask. El backend de TopoField nunca pudo leer esas tablas. Todo el inventario de Fase 0/ADR 001 mezcló los dos proyectos sin darse cuenta. **Decisión de Erick: reconocer el error, no migrar `obras`, diseñar el modelo de faenas desde cero dentro de `topofield`.** Al revisar qué existe ya en `topofield` para partir de ahí (no desde cero de verdad): `projects`/`stations` son el modelo real y vivo (con endpoints ya montados, incluida importación CSV de catálogo de códigos), y hay una migración completa sin aplicar — `apps/backend/migrations/deprecated/014_monitoring_rounds.sql` (`instrument_types`, `control_points`, `monitoring_rounds`, `monitoring_round_points`, `instrument_readings`, `reading_attachments`, `control_point_thresholds`, `project_code_catalog`, `project_rules`) — diseñada específicamente sobre la `projects` real, con idempotencia (`client_request_id` UNIQUE) y lógica de umbrales ya escrita y con tests en verde (`monitoring-reading-evaluation.ts`). Ya hay código de controller/model parcial (`monitoring.controller.ts`, `monitoring.model.ts`) con 2 endpoints montados (crear round point, crear lectura); faltan el resto (crear ronda, listar rondas/puntos pendientes, histórico). Decisión pendiente de Erick: adoptar y terminar esto, o diseñar otra cosa — no se ha tocado código de Fase 3 todavía.
- **2026-07-30 — Fase 3 backend: adopción y ampliación de monitoring_rounds (Cowork):** Erick confirmó adoptar el candidato de la entrada anterior ("tomo tu mejor opción, sigamos"). Antes de escribir código se revisó `deprecated/README.md`, que documentaba dos objeciones al descarte además del proyecto equivocado: (1) `instrument_readings` usa un blob genérico `valueNumeric|valueText+unit`, señalado en su momento como el antipatrón que el plan maestro advierte evitar; (2) sobreingeniería prematura. Sobre (1): se mantiene, porque para instrumentos escalares (nivel digital, piezómetro, distanciómetro, inclinómetro, regla de peralte) un valor+unidad es una representación correcta — el antipatrón real sería forzar ahí algo vectorial como estación total, y por eso se quita `total_station` del catálogo `instrument_types` (ya tiene tabla propia, `prism_monitoring_observations`, migración 005 — habría sido un solape real). Sobre (2): la objeción original comparaba contra `obras`, que resultó ser una base de datos ajena; sin ese comparador, rondas/puntos/umbrales son exactamente lo que pide `PLAN.md` para el MVP de auscultación, no sobreingeniería. Contenido copiado a `apps/backend/migrations/019_monitoring_rounds.sql` (sin aplicar a Supabase), con nota en `deprecated/014...sql` y `deprecated/README.md` señalando la reactivación. Backend ampliado: `monitoring-validation.ts` (esquemas para crear ronda, listar rondas, crear/actualizar punto de control, crear umbral, histórico de lecturas), `monitoring.model.ts` (`createMonitoringRound`, `listMonitoringRounds`, `getMonitoringRoundDetail`, `createControlPoint`, `listControlPoints`, `updateControlPoint`, `createControlPointThreshold`, `listControlPointThresholds`, `getReadingHistory`), `monitoring.controller.ts` (controladores equivalentes) y rutas nuevas: `GET/POST /api/v1/projects/:projectId/rounds`, `GET/POST /api/v1/projects/:projectId/control-points`, `GET /api/v1/rounds/:roundId`, `PATCH /api/v1/control-points/:controlPointId`, `GET/POST /api/v1/control-points/:controlPointId/thresholds`, `GET /api/v1/control-points/:controlPointId/readings`. Un bug real de parámetros SQL (offset de `$` mal calculado en `createControlPoint`, habría chocado con el parámetro `notes`) se detectó y corrigió antes de ejecutar nada, al contar a mano los placeholders. Verificación: copia a disco local rápido (no la carpeta FUSE), `npm install`, `tsc -p tsconfig.json` limpio, `node --test ./dist/lib/all.test.js` → **27/27** (los 2 fallos iniciales eran aserciones de test mal escritas por Cowork, esperaban el mensaje del `refine` de zod en vez del mensaje fijo del `AppError` — se corrigieron los tests, no el código). CRLF corregido en los 9 archivos tocados (`sed -i 's/\r$//'`), confirmado con `git diff --ignore-space-at-eol --stat` idéntico antes/después. Pendiente: aplicar migración 019 a Supabase (requiere autorización explícita de Erick, como en anteriores), decidir uso de `project_code_catalog`/`project_rules` (ya cubierto en parte por el catálogo de códigos existente vía `projects.routes.ts`), pantallas móviles.
- **2026-07-30 — Migración 019 aplicada a Supabase, RLS desactivado en las 9 tablas nuevas (Cowork):** Erick autorizó ("aplícala"). Verificado antes de aplicar: proyecto `tmlexrsnxpmykbpeebri` (topofield) es el correcto, ninguna de las 9 tablas existía, FKs (`projects`, `users`) con datos. `apply_migration` devolvió éxito; `list_tables` confirma las 9 tablas creadas y `instrument_types` sembrado con 6 filas (sin `total_station`); `list_migrations` confirma el registro `20260730201149__019_monitoring_rounds`. **El advisor de seguridad de Supabase marca `rls_disabled` como `critical` para las 9 tablas nuevas** (`instrument_types`, `control_points`, `monitoring_rounds`, `monitoring_round_points`, `instrument_readings`, `reading_attachments`, `control_point_thresholds`, `project_code_catalog`, `project_rules`) — las 37 tablas restantes del proyecto sí tienen RLS activo (verificado en §4 de este documento el 26-07-2026), así que esto es una regresión puntual de esta migración, no un problema preexistente. No se aplicó el `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` de remedio porque activar RLS sin políticas bloquea todo acceso — decidir las políticas es equivalente en esfuerzo a decidir cómo se expone cada tabla, y no es una decisión que deba tomar sola. Precisión sobre el riesgo real: el backend conecta a Postgres directo vía `DATABASE_URL` (`apps/backend/src/db/pool.ts`, rol con `BYPASSRLS` propio de Supabase), así que la API Express actual no depende de RLS y sigue funcionando igual con o sin él. El riesgo es otro: Supabase expone automáticamente **todas** las tablas de `public` vía su API REST (PostgREST) usando `SUPABASE_ANON_KEY` — independientemente de si el código de este repo usa esa vía o no (el móvil no usa `supabase-js` directamente; el backend sí instancia un cliente `supabaseAnon` en `lib/supabase.ts`, aunque parece ser solo para auth). Con RLS desactivado, cualquiera con esa anon key (pensada para ser pública) puede leer/escribir todas las filas de las 9 tablas nuevas por HTTP directo a Supabase, sin pasar por `assertProjectAccess` ni por el backend. Es el mismo motivo por el que las otras 37 tablas del proyecto sí tienen RLS activo.
- **2026-07-30 — Cowork cede el resto de Fase 3 a Codex (límite de tokens de Erick):** backend de Fase 3 queda cerrado (migraciones 019+020 aplicadas, RLS activo, 27/27 tests). Falta: pantallas móviles (rondas, captura de lectura) y decidir uso de `project_code_catalog`/`project_rules`. Prompt de arranque entregado a Erick para pegar en Codex CLI; no incluido aquí para no duplicar — Codex debe registrar su propia fila en §9 al empezar, por regla de `AGENTS.md`.
- **2026-07-30 — Limpieza de documentación pedida por Erick ("vamos hacia adelante y hacia atrás por documentación"):** además de diseñar las políticas RLS, se hizo una pasada de higiene documental con hallazgos concretos, no un repaso genérico. (1) `.gitattributes` no existía — causa raíz de que `git status` mostrara decenas de archivos "modificados" solo por CRLF/LF mezclado en cada edición; se añadió (`* text=auto eol=lf` + binarios explícitos jpg/png/ttf/apk/etc.) y se corrió `git add --renormalize .` (no encontró nada que renormalizar en ese momento — el árbol de trabajo ya coincidía con HEAD; este entorno ya mostró antes vistas inconsistentes de archivos sin commitear, ver entrada 2026-07-26 "falsa alarma git status"). (2) `NEXT_CHAT_HANDOFF.md` tenía 423 líneas, la mayoría de antes del 2026-06-07: citaba `phase-2-device-e2e` como rama "actual" cuando ya se fusionó y se borró, y el bloque "Para Continuar en Codex Cloud" hablaba de prioridades de hace un mes. Se reescribió el contexto rápido y se añadió la actualización de Fase 3; el resto (historial de APKs/EAS builds/bugs de junio) quedó marcado explícitamente como archivo histórico anterior al rework, sin borrarlo. (3) `FASE_0_INVENTARIO_COMPLETO.md` y `FASE_1_RECOMENDACIONES.md` (24 menciones de `obras`/`campanas` cada uno) asumen que esas tablas viven en `topofield` — falso; banner de corrección añadido apuntando a ADR 001. (4) `FASE_0_INVENTARIO_RAW.txt` dio un hallazgo técnico real, no solo una corrección: muestra `campanas` con 7 filas "en topofield", pero `apps/backend/src/scripts/inventory-supabase-schema.ts` línea 79 imprime el texto fijo `"(topofield)"` en el log sin comprobar contra qué proyecto conectó realmente `DATABASE_URL` (línea 32) — la etiqueta no prueba nada. Esto explica el mecanismo probable del error original de ADR 001 (la variable de entorno activa el 26-07-2026 probablemente apuntaba a `topotask-backend`), no solo "alguien se confundió". (5) `LAUNCH_PLAN.md`/`PRODUCT_STRATEGY.md`/`UX_RESEARCH_PLAN.md` revisados: las menciones de "obras"/"jornadas" ahí son uso coloquial normal (obra de construcción, jornada de trabajo), no la tabla descartada — sin contradicción real, no se tocaron. (6) Políticas RLS para las 9 tablas de `monitoring_rounds`: antes de diseñar nada se verificó con `pg_policies` que las 15 tablas existentes con RLS activo usan todas la misma política única, `"legacy deny all"` (`anon, authenticated`, `FOR ALL`, `USING (false)`) — no hay scoping por proyecto a nivel de Postgres, porque el backend nunca consulta estas tablas vía PostgREST/anon key (usa `DATABASE_URL` directo, que bypassa RLS, y `supabaseAdmin` con service role solo para verificar tokens de auth). El diseño correcto no es inventar políticas por rol/proyecto — es replicar la política ya usada en todo el proyecto. Migración `apps/backend/migrations/020_monitoring_rounds_rls.sql` creada con esa política para las 9 tablas nuevas, **sin aplicar** — pendiente confirmación explícita de Erick antes de tocar Supabase, igual que las migraciones anteriores.
- **2026-07-31 — Decisión Fase 3 móvil: lecturas offline-first (Codex):** las lecturas de auscultación entrarán por SQLite/outbox/sync-engine, no por un POST directo. Justificación: son precisamente datos de campo con cobertura irregular, el backend exige `clientRequestId` para idempotencia y el mismo patrón quedó validado en Galaxy en Fase 2. La UI mostrará el estado local pendiente/sincronizado y el umbral se aplicará al recibir la respuesta del backend; no se altera el backend ni Supabase.
- **2026-07-31 — Pantallas móviles de Fase 3 implementadas (Codex):** creados los flujos de rondas, puntos de control, historial y captura de lectura con outbox `medicion`; TypeScript limpio, Jest móvil `30/30` y export Android correcto. Hallazgo de integración: la URL de Render configurada por el móvil devuelve `404` para `/projects/:projectId/rounds`, por lo que Supabase tiene Fase 3 pero Render aún no publica las rutas Express; no se desplegó ni se tocó producción. La foto de lectura queda pendiente porque falta endpoint de `reading_attachments`.
- **2026-07-31 — Adjuntos de lectura implementados localmente (Codex):** añadido `POST /api/v1/round-points/:roundPointId/readings/:readingId/attachments`, firmado de Storage específico para lectura, validación de ruta `readings/<readingId>/<uploadId>`, scope por proyecto y reintento idempotente de foto desde outbox persistente; backend `30/30`, móvil `31/31`, TypeScript limpio y rutas locales devuelven `401` sin bearer / `200` con visitante para rondas. `main` se publicó en GitHub (`5709430..1abf2a1`), pero Render continúa en `2fd2eb2` y devuelve `404`: falta disparar o corregir el deploy en Render antes del E2E público.
- **2026-07-31 — Evidencia final de Render (Codex):** tras integrar y publicar `149ad67`, tres consultas separadas a `/api/v1/health` devolvieron el mismo commit antiguo `2fd2eb2fab825f3d9df84dfa631d037ac0608e67`; la ruta de rondas siguió en `404`. GitHub sí tiene `main` actualizado y la consulta autenticada de hooks convencionales devolvió `0`, por lo que no hay evidencia de un redeploy automático activo. No se puede disparar manualmente sin acceso o deploy hook de Render.
- **2026-07-31 — Build de Render reparado (Codex):** el servicio compila desde `apps/backend`, cuyo `package-lock.json` separado había quedado en esbuild 0.28.0 aunque `package.json` ya exige 0.28.1. Se regeneró ese lock local con `--workspaces=false`; `npm ci` y `npm ci --omit=dev` con npm 10.9.8 pasan, igual que build y 30/30 tests backend. No se tocó Supabase.
- **2026-07-31 — Regresión de permisos detectada en Galaxy (Codex):** la primera creación real de una ronda con sesión `topografo` devolvió error 5xx. Causa: `createMonitoringRound` y `createControlPoint` consultan la tabla `projects` bajo scope, pero el helper generaba `p.project_id`; esa columna no existe, el nombre correcto ahí es `p.id`. Se parametrizó explícitamente la columna de scope, sin alterar las consultas de tablas hijas que sí usan `project_id`, y se añadieron dos pruebas de regresión. Resultado local: backend compila y `32/32` tests pasan; pendiente publicar el arreglo en Render y repetir el flujo real.
- **2026-07-31 — E2E Fase 3 completado en Galaxy y defectos corregidos (Codex):** se publicó primero el arreglo de scope (`0c61b50`) y después `e7e2902`, confirmado por `/health` de Render. La prueba final se hizo con build Android local por Gradle, sin EAS: lectura `7 mm` con foto guardada estando `airplane_mode_on=1`, `force-stop`/reapertura aún sin conectividad, y recuperación automática de red. Logcat registró el UUID `a0ee5b77-480d-4f3e-9ed5-7ee8bb4810bb` y `Flush complete: 2/2 synced`; consulta de solo lectura en Supabase confirmó exactamente una lectura con ese UUID y un adjunto. Durante una repetición anterior apareció un `500` al reintentar un adjunto: la rama idempotente enviaba `Date.toString()` (zona `GMT+0200` no válida para PostgreSQL); se corrigió a ISO 8601 y se cubrió con test. También se corrigió el polling del sync engine para reintentar operaciones transitorias aunque la conectividad no cambie. Verificación tras el arreglo: backend build y **33/33** tests; sync engine móvil **13/13**; TypeScript móvil limpio. Pendientes reales, no bloqueantes para el motor: tras un arranque frío sin red la pantalla Obras no tiene aún caché y muestra que no puede cargar; los elementos de outbox en `error` no exponen todavía un reintento/diagnóstico en UI. No se aplicó ninguna migración ni se borraron datos de prueba de Supabase. Informe completo: `PHASE_3_DEVICE_E2E_REPORT_2026-07-31.md`.
- **2026-07-31 — Diagnóstico previo de re-login tras inactividad (Codex):** `SessionProvider` sí persiste `refreshToken` en SecureStore y llama a `/auth/refresh` al arrancar si el JWT de acceso ya venció; no hay evidencia de que el refresh token haya expirado por configuración de Supabase. El defecto de la app está en el `catch` de ese refresh: un timeout/red/arranque en frío de Render sustituye y persiste el token por cadena vacía, por lo que el siguiente arranque exige re-login aunque el refresh token siga siendo válido. Se corregirá preservando la sesión ante error transitorio; un `401 INVALID_REFRESH_TOKEN` seguirá exigiendo login. No se cambia configuración de Supabase Auth.
- **2026-07-31 — Pendientes prioritarios de piloto resueltos/documentados (Codex):** `SessionProvider` ya distingue rechazo explícito de refresh (`401 INVALID_REFRESH_TOKEN`) de fallo transitorio y no destruye SecureStore en el segundo caso; la prueba cubre ambos. SQLite incorpora `project_list_cache` por sesión local y `useProjects` devuelve la última lista válida con indicador de datos sin actualizar. Perfil expone elementos `error` de la outbox, su motivo, contador y reintento que llama al flush real. Se verificó que el catálogo por proyecto de rutas ya es `project_code_catalog`, y que `project_rules` alimenta `auto_confirm_green`; se mantienen ambos. Consulta de solo lectura a `pg_policies` generó `apps/backend/migrations/baseline/legacy_rls_baseline.sql`: hay 24 políticas `legacy deny all` actuales, no 37. No se aplicó SQL, migración remota ni configuración Auth. Verificación: móvil TypeScript limpio, **35/35** tests y export Android local; backend build y **33/33** tests.
